<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tang.component.find" >

    <application
        android:name="com.tang.common.debug.ModuleTestApplication"
        android:allowBackup="true"
        android:icon="@drawable/module_main_icon_tab_home_select"
        android:label="@string/module_main_tab_home"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.NoTitleBar">
        <activity android:name=".base.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
